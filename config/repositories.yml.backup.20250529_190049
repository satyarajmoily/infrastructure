# AI Agent Team Platform - Repository Configuration
# This file persists across restarts and defines all target repositories

platform:
  name: "AI Agent Team Platform"
  version: "1.0.0"
  description: "Generic AI agent team that can manage any repository"

target_repositories:
  market-predictor:
    github_url: "https://github.com/satyarajmoily/market-predictor.git"
    type: "fastapi"
    port: 8000
    health_endpoint: "/health"
    metrics_endpoint: "/metrics"
    coding_enabled: true
    monitoring_enabled: true
    auto_recovery: true
    description: "Bitcoin price prediction service"
    added_date: "2024-01-15"
    
  # Additional repositories can be added via scripts/add-repository.sh
  # Example:
  # client-webapp:
  #   github_url: "https://github.com/client/webapp.git"
  #   type: "react"
  #   port: 3000
  #   health_endpoint: "/health"
  #   coding_enabled: true
  #   monitoring_enabled: false
  #   added_date: "2024-01-16"

# Repository type configurations
repository_types:
  fastapi:
    default_port: 8000
    health_endpoint: "/health"
    metrics_endpoint: "/metrics"
    dockerfile_path: "Dockerfile"
    test_command: "pytest"
    
  react:
    default_port: 3000
    health_endpoint: "/health"
    dockerfile_path: "Dockerfile"
    test_command: "npm test"
    
  python:
    default_port: 8080
    health_endpoint: "/status"
    test_command: "python -m pytest"
    
  nodejs:
    default_port: 3000
    health_endpoint: "/health"
    dockerfile_path: "Dockerfile"
    test_command: "npm test" 